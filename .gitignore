*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# build outputs at any level (app/build contains APKs)
build/

# kotlin session caches
.kotlin/

# local artifacts that must never be published
*.apk
*.jks
*.keystore
credentials*.yaml

# the companion dsh-web-all plugin is NOT part of this project
plugin-src/
dsh-web-*.zip
.ver-check/

# but the release APK is published in-repo by explicit decision
!release/*.apk
